# ── Moodle Database Connection ──
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=moodle
DB_PREFIX=mdl_

# ── MongoDB Cache ──
MONGO_URI=mongodb://localhost:27017/moodle_cache

# ── Server ──
PORT=3001
